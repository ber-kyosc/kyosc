{% extends 'email/base.html.twig' %}

{% block title %}
    Invitation à rejoindre un clan
{% endblock %}

{% block content %}
    <h1>Vous avez été invité à rejoindre une clan KYOSC !</h1>
    <p>Hello, vous avez été convié·e·s par {{ user.firstName }} {{ user.lastName }} à rejoindre le clan "{{ clan.name }}".</p>
    <p>Afin d'accepter ou refuser cette invitation, il vous faut vous connecter ou créer un compte sur la plateforme KYOSC :</p>
    <div class="m-3">
        <a href="{{url('app_login') }}" class="btn btn-success text-uppercase text-white mx-2">Se connecter</a>
        <a href="{{url('app_register') }}" class="btn btn-warning text-uppercase text-white mx-2">Créer un compte</a>
    </div>
    <p>Pour voir les détails du clan en question:</p>
    <div class="m-3">
        <a href="{{url('clan_show', {id: clan.id}) }}" class="btn btn-info">Voir le clan</a>
    </div>
{% endblock %}
