{% extends 'base.html.twig' %}

{% block title %}Clan{% endblock %}

{% block body %}
    <h1>Clan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ clan.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ clan.logo }}</td>
            </tr>
            <tr>
                <th>Banner</th>
                <td>{{ clan.banner }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ clan.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clan_index') }}">back to list</a>

    <a href="{{ path('clan_edit', {'id': clan.id}) }}">edit</a>

    {{ include('clan/_delete_form.html.twig') }}
{% endblock %}
