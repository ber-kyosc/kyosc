<nav class="navbar navbar-default navbar-dark bg-dark">
    <button class="navbar-toggler" type="button">
        <span class="navbar-toggler-icon"></span>
    </button>
    <ul class="sidenav">
        {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path("admin") }}">Espace administrateur</a>
            {% endif %}
        {% endif %}
        <li> <a href="{{ path("home") }}">Accueil</a> </li>
        <li> <a href="{{ path("challenge_index") }}">Les challenges</a> </li>
        {% if app.user %}
            <li> <a href="{{ path("profil_my_profil") }}">Mon Espace</a> </li>
            <li> <a href="{{ path("challenge_new") }}">Nouveau Challenge</a> </li>
            <li> <a href="{{ path("app_logout") }}">Déconnexion</a> </li>
        {% else %}
            <li> <a href="{{ path("app_login") }}">Se connecter</a> </li>
        {% endif %}
    </ul>
    {% if app.user  %}
        <span class="text-white"> Bonjour {{ app.user.pseudo? app.user.pseudo : app.user.firstname ~' '~ app.user.lastname}}</span>
    {% endif %}
    <a class="navbar-brand" href="{{ path("home") }}">
        <img src="{{ sportLogo }}" alt="Logo KYOSC" class="logo">
    </a>
</nav>
<div class="nav-menu-overlay"></div>
