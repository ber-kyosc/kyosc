<nav class="navbar navbar-default navbar-dark bg-dark">
    <button class="navbar-toggler" type="button">
        <span class="navbar-toggler-icon"></span>
    </button>
    <ul class="sidenav">
        {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path("admin") }}">Espace administrateur</a>
            {% endif %}
        {% endif %}
        <li> <a href="{{ path("home") }}">Accueil</a> </li>
        <li> <a href="{{ path("challenge_index") }}">Les aventures</a> </li>
        <li> <a href="{{ path("clan_index") }}">Les clans</a> </li>
        {% if app.user %}
            <li> <a href="{{ path("profil_my_profil") }}">Mon espace</a> </li>
            <li> <a href="{{ path("challenge_new") }}">Nouvelle aventure</a> </li>
            <li> <a href="{{ path("clan_new") }}">Nouveau clan</a> </li>
            <li> <a href="{{ path("app_logout") }}">Déconnexion</a> </li>
        {% else %}
            <li> <a href="{{ path("app_login") }}">Se connecter</a> </li>
        {% endif %}
    </ul>
    {% if app.user  %}
        <a href="{{ path("profil_show", {id: app.user.id}) }}" class="w-25 text-center">
            {% if app.user.profilPhoto is not null%}
                <img src="{{ vich_uploader_asset(app.user, 'profilPhotoFile') | imagine_filter('avatar_thumb') }}" alt="{{ app.user.profilPhoto }}" class="avatar-testimony">
            {% else %}
                <span class="text-white text-wrap"> Bonjour {{ app.user.pseudo? app.user.pseudo : app.user.firstname ~' '~ app.user.lastname}}</span>
            {% endif %}
        </a>
    {% endif %}
    <a class="navbar-brand" href="{{ path("home") }}">
        <img src="{{ asset(defaultLogo) }}" alt="Logo KYOSC" class="logo">
    </a>
</nav>
<div class="nav-menu-overlay"></div>
